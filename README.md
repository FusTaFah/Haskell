# SVG creation with Haskell

A trippy SVG animation

## Requirements

in prelude,

```prelude
:cd <absolute local path to repo>
:load MyAnimation
test
````

This will create test.svg which you can view using a browser (IE or Chrome preferred)